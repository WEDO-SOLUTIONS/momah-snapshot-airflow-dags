# Ignore ONLY these files — not needed by Airflow
.airflowignore
etl_requirements.txt

# Ignore helper Python scripts so Airflow does NOT treat them as DAGs
snapshot_pro_etl/common/*.py
snapshot_pro_etl/mappers/*.py

# But DO NOT ignore the directories — Python still needs them to resolve imports
